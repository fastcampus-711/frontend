nameOverride: ""
fullnameOverride: ""

replicaCount: 1
application:
  port: 3000
  profile: dev

service:
  type: ClusterIP
  port: 80
  targetPort: 3000
  nodePort:

serviceAccount:
  create: false
  annotations: {}
  name: ""

image:
  repository: harbor.ha-ving.store/docker/711-front
  tag: "6822df824680b15c2c133b3315aa0069d4b33a7d"
  pullPolicy: IfNotPresent

podAnnotations: {}
podSecurityContext: {}
securityContext: {}

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 60
  targetMemoryUtilizationPercentage: 60

ingress:
  enabled: true
  ingressClassName: "nginx"
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "ha-ving.store-issuer"
  hosts:
    - host: aptner.ha-ving.store
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: aptner-ingress
      hosts:
        - aptner.ha-ving.store

resources:
  limits:
    cpu: 2000m
    memory: 2048Mi
  requests:
    cpu: 1000m
    memory: 1024Mi

volumes: []
# - name: foo
#   secret:
#     secretName: mysecret
#     optional: false

nodeSelector: {}

tolerations: []

affinity: {}